DATE_FROM="2025.09.25"
DATE_TO="2025.01.02"

while IFS=';' read -r USER PASS
do
  echo ">>> $USER"
  printf "%s\n%s\n" "$DATE_FROM" "$DATE_TO" | \
  sqlplus -s "$USER/$PASS@DB" @statistic.sql
done < users.txt